#include <stdio.h>

/**
 * main - prints single numeber 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 28; num < 38; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
