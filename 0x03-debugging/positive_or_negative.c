#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Checks if number is positive or negative.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	return (0);
}
